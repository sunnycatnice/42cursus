#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>

int main()
{
		int i =13435;
		char *str = "annamaria";
		ft_printf("sono d %d sono s %s sono x %x", i, str, i);
		printf("sono d %d sono s %s sono x %x", i, str, i);
}
